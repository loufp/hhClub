# 🚀 Полная автоматизированная инфраструктура CI/CD

Этот документ описывает **полностью автоматизированный** запуск локальной инфраструктуры CI/CD с GitLab CE, GitLab Runner, SonarQube, Nexus и Jenkins с предварительно настроенными интеграциями.

## 📋 Требования

- **Docker** и **docker-compose** (версии 20.10+)
- **jq** для обработки JSON (`brew install jq` на macOS)
- Доступ к портам: 9080, 9443, 2224, 8080, 9000, 8181
- ~4GB RAM для всех сервисов

## 🎯 Быстрый запуск (одна команда)

```bash
# В корне репозитория
chmod +x ci-scripts/*.sh
./ci-scripts/start_infrastructure.sh
```

**Это всё!** Скрипт автоматически:
- Поднимет все сервисы (GitLab, Runner, SonarQube, Nexus, Jenkins)
- Создаст root access token в GitLab через Rails console
- Создаст группу `ci-cd-samples` и примеры проектов для 4 языков
- Зарегистрирует Docker-in-Docker runner с privileged доступом
- Настроит CI/CD переменные (SonarQube, Nexus URLs, credentials)
- Создаст SonarQube токен и настроит интеграцию
- Добавит готовые `.gitlab-ci.yml` в каждый проект

## 📊 После запуска доступны сервисы

| Сервис | URL | Логин/Пароль |
|--------|-----|--------------|
| 🦊 **GitLab CE** | http://localhost:9080 | root / ChangeMe123! |
| 📊 **SonarQube** | http://localhost:9000 | admin / admin |
| 📦 **Nexus** | http://localhost:8181 | admin / admin123 |
| 🔨 **Jenkins** | http://localhost:8080 | (unlock key в логах) |

## 🧪 Тестирование CI/CD

1. **Откройте GitLab**: http://localhost:9080
2. **Войдите как root** (пароль: `ChangeMe123!`)
3. **Перейдите в группу** `ci-cd-samples`
4. **Выберите любой проект**:
   - `sample-java` - Maven + Spring Boot pipeline
   - `sample-go` - Go modules pipeline  
   - `sample-nodejs` - npm + Node.js pipeline
   - `sample-python` - pip + pytest pipeline
5. **Внесите изменение** в код или `.gitlab-ci.yml`
6. **Commit & Push** → pipeline автоматически запустится
7. **Проверьте**: CI/CD → Pipelines

## 🔧 Что настроено автоматически

### GitLab Runner
- ✅ Docker-in-Docker executor с privileged правами
- ✅ Подключение к docker:dind сервису
- ✅ Доступ к Docker registry и build функций
- ✅ Теги: `docker`, `dind`, `privileged`

### CI/CD переменные (Group level)
- `SONAR_HOST=http://sonarqube:9000`
- `SONAR_TOKEN` (автоматически созданный)
- `NEXUS_URL=http://nexus:8181`
- `NEXUS_USER=admin`, `NEXUS_PASSWORD=admin123`
- `DOCKER_REGISTRY=localhost:5000`
- `DOCKER_HOST=tcp://docker-dind:2376`

### Готовые .gitlab-ci.yml
Каждый проект содержит pipeline с stages:
- **build** - компиляция/сборка под конкретный язык
- **test** - запуск тестов  
- **sonar** - анализ кода через SonarQube
- **docker** - сборка Docker образа
- **deploy** - деплой (placeholder)

### SonarQube интеграция
- ✅ Автоматически созданный токен для GitLab
- ✅ Webhook настроен (опционально)
- ✅ Готовые правила анализа кода

## 🐛 Troubleshooting

### Сервисы не стартуют
```bash
# Проверить логи
docker-compose logs -f gitlab
docker-compose logs -f gitlab-runner

# Перезапустить сервисы
docker-compose restart gitlab gitlab-runner
```

### Runner не регистрируется
```bash
# Проверить статус runner
docker exec gitlab-runner gitlab-runner list

# Перерегистрировать
docker exec gitlab-runner gitlab-runner unregister --all-runners
./ci-scripts/gitlab_setup.sh
```

### Pipeline не запускается
1. Проверьте Group → CI/CD → Runners - должен быть активный runner
2. Проверьте синтаксис `.gitlab-ci.yml` в проекте
3. Убедитесь что есть commits в main ветке

### Недостаточно памяти
```bash
# Увеличить лимиты Docker или остановить ненужные сервисы
docker-compose stop jenkins  # если Jenkins не нужен
docker system prune -a       # очистить неиспользуемые образы
```

## 🔒 Безопасность (для production)

⚠️ **Текущие настройки предназначены ТОЛЬКО для локального тестирования!**

Для production использования:
1. Смените все пароли по умолчанию
2. Настройте HTTPS/TLS сертификаты  
3. Используйте external volumes для backup
4. Настройте resource limits в docker-compose
5. Включите authentication для Docker registry

## 📁 Структура файлов

```
ci-scripts/
├── start_infrastructure.sh    # Главный скрипт запуска
├── gitlab_setup.sh           # Настройка GitLab + Runner  
├── sonar_integration.sh      # Настройка SonarQube
└── upload_artifact.sh        # Загрузка артефактов

docker-compose.yml            # Все сервисы инфраструктуры
```

## 🎯 Следующие шаги

После успешного запуска можно:
1. **Протестировать генерацию** CI/CD конфигураций основной системой
2. **Интегрировать** сгенерированные `.gitlab-ci.yml` с этой инфраструктурой
3. **Расширить** шаблоны для дополнительных языков и фреймворков
4. **Настроить** artifact uploading в Nexus/Docker Registry

---

🎉 **Готово!** У вас теперь есть полностью функционирующая CI/CD инфраструктура для разработки и тестирования.

